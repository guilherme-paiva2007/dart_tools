part of '../../features.dart';

// abstract interface class DatabaseModel<M extends Model<M>> {
//   DatabaseModel._();
// }

// abstract interface class DatabaseService<M extends Model<M>> {
//   DatabaseService._();

//   @mustBeOverridden
//   void delete(Id id);
// }

// base mixin FirebaseModel<M extends Model<M>> on Model<M> implements DatabaseModel<M> {}

// base mixin FirebaseService<M extends Model<M>> on Service<M> implements DatabaseService<M> {

// }